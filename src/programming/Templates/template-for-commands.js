module.exports = {
    name: `name`,
    aliases: ['n'],
    cooldown: 2,
    permissions: 'MANAGE_GUILD',
    dev: false,
    async execute(fs, creators, message, client, args, Discord, lang, target, betamode, inventory, prefix, usersettings, ticketU, ticketG, serversettings, notepad, warns) {






    }
};